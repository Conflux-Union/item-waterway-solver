# Probe item horizontal collision against bamboo with gravity disabled so the
# second tick isolates bamboo collision and offset semantics after the summon
# tick materializes the item entity.
tick freeze
kill @e[type=minecraft:item,tag=codex_probe]
forceload add 0 0
fill 0 63 0 4 68 2 air
setblock 1 64 1 sand
setblock 1 65 1 bamboo[age=0,leaves=none,stage=0]
summon item 0.75 65.2 1.5 {Tags:["codex_probe"],NoGravity:1b,PickupDelay:32767,Motion:[1.0d,0.0d,0.0d],Item:{id:"minecraft:stone",count:1}}
tick step 1
data get entity @e[type=minecraft:item,tag=codex_probe,limit=1]
tick step 1
data get entity @e[type=minecraft:item,tag=codex_probe,limit=1]
